{
  "type": "object",
  "additionalProperties": false,
  "required": ["title", "tag", "overallScore", "highlights", "coreGoal", "synergyCombo", "summary", "warnings", "steps", "inventoryRecommendations"],
  "properties": {
    "title": {"type": "string", "minLength": 1, "maxLength": 40},
    "tag": {"type": "string", "minLength": 1, "maxLength": 30},
    "overallScore": {"type": "integer", "minimum": 0, "maximum": 100},
    "highlights": {"type": "array", "minItems": 1, "maxItems": 2, "items": {"type": "string", "maxLength": 80}},
    "coreGoal": {"type": "string", "minLength": 1, "maxLength": 80},
    "synergyCombo": {"type": "string", "minLength": 1, "maxLength": 80},
    "summary": {"type": "string", "minLength": 1, "maxLength": 500},
    "warnings": {"type": "array", "maxItems": 6, "items": {"type": "string", "maxLength": 160}},
    "steps": {
      "type": "array",
      "minItems": 1,
      "maxItems": 12,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["order", "matchScore", "matchSummary", "safetyLevel", "safetyTitle", "safetySummary", "reasons", "estimatedIngredientCount", "ingredients"],
        "properties": {
          "order": {"type": "integer", "minimum": 1, "maximum": 12},
          "matchScore": {"type": "integer", "minimum": 0, "maximum": 100},
          "matchSummary": {"type": "string", "minLength": 1, "maxLength": 120},
          "safetyLevel": {"type": "string", "enum": ["SAFE", "CAUTION", "WARNING", "UNKNOWN"]},
          "safetyTitle": {"type": "string", "minLength": 1, "maxLength": 80},
          "safetySummary": {"type": "string", "minLength": 1, "maxLength": 240},
          "reasons": {
            "type": "array",
            "minItems": 1,
            "maxItems": 3,
            "items": {
              "type": "object",
              "additionalProperties": false,
              "required": ["tone", "title", "description", "evidenceSource"],
              "properties": {
                "tone": {"type": "string", "enum": ["POSITIVE", "NEUTRAL", "CAUTION", "WARNING"]},
                "title": {"type": "string", "minLength": 1, "maxLength": 80},
                "description": {"type": "string", "minLength": 1, "maxLength": 240},
                "evidenceSource": {"type": "string", "minLength": 1, "maxLength": 80}
              }
            }
          },
          "estimatedIngredientCount": {"type": "integer", "minimum": 0, "maximum": 200},
          "ingredients": {
            "type": "array",
            "maxItems": 6,
            "items": {
              "type": "object",
              "additionalProperties": false,
              "required": ["name", "purposes"],
              "properties": {
                "name": {"type": "string", "minLength": 1, "maxLength": 80},
                "purposes": {"type": "array", "maxItems": 3, "items": {"type": "string", "maxLength": 80}}
              }
            }
          }
        }
      }
    },
    "inventoryRecommendations": {
      "type": "array",
      "minItems": 1,
      "maxItems": 12,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["order", "inventoryId", "reason"],
        "properties": {
          "order": {"type": "integer", "minimum": 1, "maximum": 12},
          "inventoryId": {"type": ["integer", "null"]},
          "reason": {"type": "string", "minLength": 1, "maxLength": 240}
        }
      }
    }
  }
}
